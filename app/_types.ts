type ChangeEvent = React.ChangeEvent<HTMLInputElement>;
type FormEvent = React.FormEvent<HTMLFormElement>;
type Errors = { msg: string; path: string };

export type { ChangeEvent, Errors, FormEvent };
